<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Tu Carrito</title>
  <style>
    body {
      margin: 0;
      padding: 2rem;
      font-family: 'Segoe UI', sans-serif;
      background: #f4f4f4;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      text-align: center;
    }

    h1 {
      margin-bottom: 2rem;
      color: #333;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 2rem;
    }

    .card {
      background: white;
      padding: 1.5rem;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    }

    .card img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      border-radius: 8px;
      margin-bottom: 1rem;
    }

    .card h3 {
      margin: 0.5rem 0;
      color: #007BFF;
    }

    .card p {
      margin: 0.3rem 0;
      color: #444;
      font-size: 0.95rem;
    }

    .price {
      font-weight: bold;
      font-size: 1.1rem;
      color: #28a745;
    }

    .card form {
      margin-top: 1rem;
    }

    .card button {
      background-color: #dc3545;
      color: white;
      border: none;
      padding: 0.6rem 1.2rem;
      border-radius: 5px;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s;
      width: 100%;
    }

    .card button:hover {
      background-color: #a71d2a;
    }

    .actions {
      margin-top: 2rem;
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .actions form button {
      background-color: #007BFF;
      padding: 0.7rem 2rem;
      border-radius: 8px;
      color: white;
      font-weight: bold;
      border: none;
      cursor: pointer;
    }

    .actions form button:hover {
      background-color: #0056b3;
    }

    .total {
      margin-top: 2rem;
      font-size: 1.3rem;
      color: #333;
      font-weight: bold;
    }

  </style>
</head>
<body>
  <div class="container">
    <h1>Tu Carrito</h1>

    {{#if cart.products.length}}
      <div class="grid">
        {{#each cart.products}}
          <div class="card">
            {{#if this.product.images.[0]}}
              <img src="{{this.product.images.[0]}}" alt="{{this.product.name}}" />
            {{else}}
              <img src="/placeholder.jpg" alt="Sin imagen" />
            {{/if}}

            <h3>{{this.product.name}}</h3>
            <p><strong>Categoría:</strong> {{this.product.category}}</p>
            <p><strong>Cantidad:</strong> {{this.quantity}}</p>
            <p class="price">$ {{this.product.price}}</p>
            <p><strong>Total:</strong> ${{this.total}}</p>

            <form method="POST" action="/api/cart/remove/{{this.product._id}}?_method=DELETE">
              <button type="submit">Eliminar</button>
            </form>
          </div>
        {{/each}}
      </div>

      <div class="total">
        Total del carrito: ${{totalCarrito}}
      </div>

      <div class="actions">
        <form method="POST" action="/api/cart/clear?_method=DELETE">
          <button type="submit">Vaciar carrito</button>
        </form>

        <form method="POST" action="/api/checkout">
          <button type="submit">Finalizar compra</button>
        </form>

        <form method="GET" action="/products">
          <button type="submit">Volver al catálogo</button>
        </form>
      </div>
    {{else}}
      <p>Tu carrito está vacío.</p>

      <div class="actions">
        <form method="GET" action="/products">
          <button type="submit">Volver al catálogo</button>
        </form>
      </div>
    {{/if}}
  </div>
</body>
</html>


